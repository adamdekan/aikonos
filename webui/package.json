{
  "name": "@aikonos/webui",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "description": "Aikonos unified web UI — Vue 3 SPA + Node proxy backend (static dist/ + gateway proxy).",
  "scripts": {
    "build": "npm --prefix web install && npm --prefix web run build",
    "start": "node server.mjs",
    "dev": "node server.mjs",
    "smoke": "node scripts/smoke.mjs",
    "test": "node --test test/proxy.test.mjs"
  },
  "dependencies": {
    "@fastify/static": "^8.0.3",
    "fastify": "^5.2.1"
  }
}
